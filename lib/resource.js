// Generated by CoffeeScript 1.7.1
(function() {
  var Handler, Resource, Router, _;

  _ = require('lodash');

  Handler = require('./handler');

  Router = require('./router');

  Resource = (function() {
    function Resource(model, path, options) {
      var default_options;
      this.model = model;
      this.path = path;
      default_options = {
        resource_key: "_id",
        page_size: 20
      };
      this.options = {};
      _.merge(this.options, default_options, options);
      this.handler = new Handler(this.model, this.path, this.options);
      this.router = new Router(this.path, this.handler, this.options);
    }

    return Resource;

  })();

  module.exports = Resource;

}).call(this);
